<% if @projects.any? %>
  <ul>
    <% @projects.each do |project| %>
      <li class = "project">
        <%= link_to project.title, project_path(project) %>
      </li>
    <% end %>
  </ul>
<% end %>
